spring:
  application:
    name: trainingtask-kotlin-spring

  datasource:
    driver-class-name: org.postgresql.Driver
    username: root
    password: root
    url: jdbc:postgresql://localhost:5432/trainingtask
